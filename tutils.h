#ifndef TUTILS_H
#define TUTILS_H

#include <string>
#include <vector>

namespace tutils
{

    const std::vector<std::string>& split(const std::string& _toSplit, const std::string &_delimeter);

}

#endif
